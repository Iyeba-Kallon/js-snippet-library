// Select elements by class name
export const selectByClass = (className) => document.querySelectorAll(`.${className}`);

// Select an element by ID
export const selectById = (id) => document.getElementById(id);
